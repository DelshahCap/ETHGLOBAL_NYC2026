# Maps a logical secret id (used in code via runtime.getSecret) to the env var(s)
# that hold the value. The value lives in .env (gitignored), never in this file.
secretsNames:
    SOCRATA_APP_TOKEN:
        - SOCRATA_APP_TOKEN
